import {StyleSheet} from 'react-native';
import {HORIZONTAL_BTN_SIZE} from '../constains/constain';

export const styles = StyleSheet.create({
  btnHeaderTab: {
    paddingHorizontal: HORIZONTAL_BTN_SIZE,
    paddingVertical: HORIZONTAL_BTN_SIZE / 4,
    margin: HORIZONTAL_BTN_SIZE / 2,
    borderRadius: 20,
  },
});
